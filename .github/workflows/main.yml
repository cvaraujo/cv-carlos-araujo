name: CI
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: ls
        run: |
          ls -la
          echo $HOME
          echo $(pwd)
      - name: Checkout to send
        uses: actions/checkout@v2
      - name: ls
        run: |
          ls -la
          echo $HOME
          echo $(pwd)

